# Jarvis integration (external service — developed separately in Codex).
# Leave as "mock" for local preview/testing.
VITE_JARVIS_PROVIDER=mock
# When connecting the real Jarvis, set:
#   VITE_JARVIS_PROVIDER=remote
#   VITE_JARVIS_API_URL=https://your-jarvis-service.example.com
VITE_JARVIS_API_URL=
