{
  "name": "physiotrack-platform",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "PhysioTrack — physiotherapy clinic host platform (React + Vite + TypeScript), recreated faithfully from the Claude Design v3 page, with replaceable Jarvis / anatomy / device integration boundaries.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.6"
  }
}
